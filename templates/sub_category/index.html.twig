{# {# {% extends 'base.html.twig' %}

{% block title %}SubCategory index{% endblock %}

{% block body %}
    <h1>SubCategory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sub_category in sub_categories %}
            <tr>
                <td>{{ sub_category.id }}</td>
                <td>{{ sub_category.name }}</td>
                <td>
                    <a href="{{ path('app_sub_category_show', {'id': sub_category.id}) }}">show</a>
                    <a href="{{ path('app_sub_category_edit', {'id': sub_category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_sub_category_new') }}">Create new</a>
{% endblock %} #}
{# 
{% extends 'base.html.twig' %}

{% block title %}SubCategory Management{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold text-primary">SubCategory List</h1>
        <a href="{{ path('app_sub_category_new') }}" class="btn btn-success shadow-sm">
            <i class="bi bi-plus-circle me-1"></i> Create New
        </a>
    </div>

    <div class="card shadow-sm border-0 rounded-3">
        <div class="card-body p-4">
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th scope="col"># ID</th>
                            <th scope="col">Name</th>
                            <th scope="col">Category</th>
                            <th scope="col" class="text-center">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for sub_category in sub_categories %}
                        <tr>
                            <td class="fw-semibold">{{ sub_category.id }}</td>
                            <td>{{ sub_category.name }}</td>
                            <td>{{ sub_category.category.name }}</td>
                            {# <td>{{ form.category }}</td> #}
                            {# <td class="text-center">
                                <a href="{{ path('app_sub_category_show', {'id': sub_category.id}) }}" 
                                   class="btn btn-sm btn-outline-info me-1">
                                    <i class="bi bi-eye"></i> Show
                                </a>
                                <a href="{{ path('app_sub_category_edit', {'id': sub_category.id}) }}" 
                                   class="btn btn-sm btn-outline-warning">
                                    <i class="bi bi-pencil-square"></i> Edit
                                </a>
                            </td>
                            {# insertion #}
                            {# <td class="text-center">
                                <a href="{{ path('app_sub_category_show', {'id': sub_category.category.id}) }}" 
                                   class="btn btn-sm btn-outline-info me-1">
                                    <i class="bi bi-eye"></i> Nom de la catégorie
                                </a>
                                
                            </td> #}
                            {# fin insertion #}
                        {# </tr>
                    {% else %}
                        <tr>
                            <td colspan="3" class="text-center text-muted py-4">
                                <i class="bi bi-inbox fs-4"></i>
                                <p class="mb-0">No records found</p>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
{% endblock %} #} 
{# {% extends 'base.html.twig' %}

{% block title %}SubCategory Management{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold text-primary">SubCategory List</h1>
        <a href="{{ path('app_sub_category_new') }}" class="btn btn-success shadow-sm">
            <i class="bi bi-plus-circle me-1"></i> Create New
        </a>
    </div>

    <div class="card shadow-sm border-0 rounded-3">
        <div class="card-body p-4">
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th scope="col">ID</th>
                            <th scope="col">Name</th>
                            <th scope="col">Category</th>
                            <th scope="col" class="text-center">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for sub_category in sub_categories %}
                        <tr>
                            <td class="fw-semibold">{{ sub_category.id }}</td>
                            <td>{{ sub_category.name }}</td>
                            <td>{{ sub_category.category.name }}</td>

                            <td class="text-center">
                                {# Voir détails de la sous-catégorie #}
                                {# <a href="{{ path('app_sub_category_show', {'id': sub_category.id}) }}" 
                                   class="btn btn-sm btn-outline-info me-1">
                                    <i class="bi bi-eye"></i> Show
                                </a>

                                {# Éditer la sous-catégorie #}
                                {# <a href="{{ path('app_sub_category_edit', {'id': sub_category.id}) }}" 
                                   class="btn btn-sm btn-outline-warning me-1">
                                    <i class="bi bi-pencil-square"></i> Edit
                                </a> #}

                                {# Voir la catégorie parente #}
                                {# <a href="{{ path('app_category', {'id': sub_category.category.name}) }}" 
                                   class="btn btn-sm btn-outline-primary">
                                    <i class="bi bi-folder"></i> Parent Category
                                </a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4" class="text-center text-muted py-4">
                                <i class="bi bi-inbox fs-4"></i>
                                <p class="mb-0">No records found</p>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
{% endblock %}
 #} 
 {# {% extends 'base.html.twig' %}

{% block title %}SubCategory index{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center mt-4 mb-4">SubCategory index</h1>
    <a href="{{ path('app_sub_category_new') }}" class="btn btn-success mb-3">Create new</a>
    <table class="table table-striped table-dark">
        <thead>
            <tr>
             <th>Subcategory</th>
                <th>Category</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sub_category in sub_categories %}
            <tr>
              </tr>
        </thead>
        <tbody>
        {% for sub_category in sub_categories %}
            <tr>
             <td>{{ sub_category.category.name }}</td>
                <td>
                  <a href="{{ path('app_sub_category_show', {'id': sub_category.id}) }}" class="btn btn-outline-success">show</a>
                    <a href="{{ path('app_sub_category_edit', {'id': sub_category.id}) }}" class="btn btn-outline-warning">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
      
        </tbody>
    </table>    
</div>

{% endblock %}  #}

{% extends 'base.html.twig' %}

{% block title %}Sub Categories{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold">Sub Categories</h1>
        <a href="{{ path('app_sub_category_new') }}" class="btn btn-primary shadow-sm">
            <i class="bi bi-plus-circle me-1"></i> Add Subcategory
        </a>
    </div>

    {# Flash messages #}
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label }} alert-dismissible fade show shadow-sm" role="alert">
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endfor %}
    {% endfor %}

    <div class="card shadow-sm rounded-4">
        <div class="card-body p-0">
            <table class="table table-hover table-striped mb-0 align-middle">
                <thead class="table-light">
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Subcategory Name</th>
                        <th scope="col">Category</th>
                        <th scope="col" class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for sub_category in sub_categories %}
                    <tr>
                        <td>{{ sub_category.id }}</td>
                        <td>{{ sub_category.name }}</td>
                        <td>{{ sub_category.category ? sub_category.category.name : '—' }}</td>
                        <td class="text-end">
                            <a href="{{ path('app_sub_category_show', {'id': sub_category.id}) }}" 
                               class="btn btn-sm btn-outline-secondary">
                                <i class="bi bi-eye"></i>
                            </a>
                            <a href="{{ path('app_sub_category_edit', {'id': sub_category.id}) }}" 
                               class="btn btn-sm btn-warning text-white">
                                <i class="bi bi-pencil"></i>
                            </a>
                            <form method="post" action="{{ path('app_sub_category_delete', {'id': sub_category.id}) }}" 
                                  style="display:inline-block"
                                  onsubmit="return confirm('Are you sure you want to delete this subcategory?');">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sub_category.id) }}">
                                <button class="btn btn-sm btn-danger">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </form>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="4" class="text-center py-4 text-muted">
                            No subcategories found.
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}

