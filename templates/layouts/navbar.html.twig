{# {# <nav class="navbar navbar-expand-lg bg-body-tertiary bg-dark" data-bs-theme="dark">
  <div class="container">
    <a class="navbar-brand" href="#">Nooriba Marrakesh</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarScroll">
        <ul class="navbar-nav me-auto my-2 my-lg-0 navbar-nav-scroll" style="--bs-scroll-height: 100px;">
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('app_home_page') }}">Home</a>
            </li>
            {% if categories is defined %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Categories
                    </a>
                    <ul class="dropdown-menu">
                        {% for category in categories %}
                            <li><a class="dropdown-item" href="#">{{ category.name }}</a></li>
                            {# {% for subCategory in category.subCategories %}
                            {# <li class="nav-item"><a href="{{ path('app_logout') }}" class="nav-link">Log out</a> #}
                    {# <li><a class="dropdown-item" href="{{ path('app_sub_category_index') }}">Subcategories</a></li>
                   </li>
                  {% endfor %} #}
               
                            {# <li><hr class="dropdown-divider"></li>
                        {% endfor %}
                    </ul>
                </li> 
            {% endif %}
            {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Admin
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_category') }}" >Categories</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_category_new') }}" >Add a Category</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('app_sub_category_index') }}" >Subcategories</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_sub_category_new') }}">Add a subcategory</a></li>
                        {% if subCategories is defined %}
                        {% for subCategory in category.subCategories %}
                        <li><a class="dropdown-item" href="{{ path('app_home_product_filter', {'id':subCategory.id}) }}">{{ subCategory.name }}</a></li>
                        {% endfor %}
                        {% endif %}

                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{path('app_product_index')}}">Products</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_product_new') }}">Add a product</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('app_user') }}">User</a></li>
                    </ul>
                </li>
                
            {% endif %}
            
                {# <li class="nav-item">
                <a class="nav-link disabled" aria-disabled="true">Disabled</a>
                </li> #}
            {# {% if app.user %}
          <li class="nav-item">
            <a class="nav-link text-white" href="{{ path('app_logout') }}">Log out</a>
          </li>
          <li class="nav-item d-flex align-items-center text-white ms-2">
            <span style="width: 10px; height: 10px; background-color: green; border-radius: 50%; display: inline-block; margin-right: 5px;"></span>
            Connecté(e) : <strong class="ms-1">{{ app.user.firstname }}</strong>
            {% if 'ROLE_ADMIN' in app.user.roles %}
              <span class="badge bg-warning text-dark ms-2">Admin</span>
            {% endif %}
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link text-white" href="{{ path('app_login') }}">Se connecter</a>
          </li>
        {% endif %}
   
            
        </ul>
        <form class="d-flex" role="search">
            <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search"/>
            <button class="btn btn-outline-success" type="submit">Search</button>
        </form>
        </div>
    </div>
</nav>  #}
{# /* <style>
/* Style des sous-menus */
.dropdown-submenu {
  position: relative;
}

.dropdown-submenu > .dropdown-menu {
  top: 0;
  left: 100%;
  margin-top: -5px;
  border-radius: 8px;
  box-shadow: 0 3px 10px rgba(0,0,0,0.15);
}

/* Hover pour desktop */
@media (min-width: 992px) {
  .dropdown-submenu:hover > .dropdown-menu {
    display: block;
  }
}

/* Sur mobile, sous-menus empilés */
@media (max-width: 991px) {
  .dropdown-submenu > .dropdown-menu {
    position: relative;
    left: 0;
    margin-left: 15px;
  }
}
</style>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <!-- Logo -->
    <a class="navbar-brand fw-bold" href="{{ path('app_home_page') }}">Nooriba Marrakesh</a>

    <!-- Bouton Burger -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar"
      aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Contenu -->
    <div class="collapse navbar-collapse" id="mainNavbar">
      <ul class="navbar-nav me-auto">

        <!-- Lien Home -->
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('app_home_page') }}">Home</a>
        </li>

        <!-- Menu Catégories -->
        {% if categories is defined %}
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="categoriesDropdown" role="button"
               data-bs-toggle="dropdown" aria-expanded="false">
              Catégories
            </a>
            <ul class="dropdown-menu" aria-labelledby="categoriesDropdown">
              {% for category in categories %}
                <li class="dropdown-submenu position-relative">
                  <a class="dropdown-item dropdown-toggle" href="#">
                    {{ category.name }}
                  </a>
                  {% if category.subCategories|length > 0 %}
                    <ul class="dropdown-menu">
                      {% for subCategory in category.subCategories %}
                        <li>
                          <a class="dropdown-item" href="{{ path('app_home_product_filter', {'id': subCategory.id}) }}">
                            {{ subCategory.name }}
                          </a>
                        </li>
                      {% endfor %}
                    </ul>
                  {% endif %}
                </li>
              {% endfor %}
            </ul>
          </li>
        {% endif %}

        <!-- Admin -->
        {% if is_granted("ROLE_ADMIN") %}
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="adminDropdown" role="button"
               data-bs-toggle="dropdown" aria-expanded="false">
              Admin
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="{{ path('app_category') }}">Categories</a></li>
              <li><a class="dropdown-item" href="{{ path('app_category_new') }}">Add a Category</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="{{ path('app_sub_category_index') }}">Subcategories</a></li>
              <li><a class="dropdown-item" href="{{ path('app_sub_category_new') }}">Add a subcategory</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="{{ path('app_product_index') }}">Products</a></li>
              <li><a class="dropdown-item" href="{{ path('app_product_new') }}">Add a product</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="{{ path('app_user') }}">User</a></li>
            </ul>
          </li>
        {% endif %}
      </ul>

      <!-- Droite : Connexion / Déconnexion -->
      <ul class="navbar-nav ms-auto">
        {% if app.user %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_logout') }}">Log out</a>
          </li>
          <li class="nav-item d-flex align-items-center text-white ms-2">
            <span class="badge bg-success me-1">&bull;</span>
            Connecté(e) : <strong class="ms-1">{{ app.user.firstname }}</strong>
            {% if 'ROLE_ADMIN' in app.user.roles %}
              <span class="badge bg-warning text-dark ms-2">Admin</span>
            {% endif %}
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
          </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav> */ ..............        NAVBAR 3  9/10 ................................#}
{# 
<nav class="navbar navbar-expand-lg navbar-dark fixed-top custom-navbar">
    <div class="container">
        <!-- Logo -->
        <a class="navbar-brand" href="{{ path('app_home_page') }}">
            <img src="{{ asset('images/logo.png') }}" alt="Logo" class="logo-navbar">
        </a>

        <!-- Burger menu -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarLuxury"
                aria-controls="navbarLuxury" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Menu -->
        <div class="collapse navbar-collapse justify-content-center" id="navbarLuxury">
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navHebergement" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Hébergement
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="#">Suites</a></li>
                        <li><a class="dropdown-item" href="#">Villas</a></li>
                    </ul>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navRestaurants" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Restaurants & Cafés
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="#">Restaurant 1</a></li>
                        <li><a class="dropdown-item" href="#">Bar Lounge</a></li>
                    </ul>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="#">Notre boutique</a>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navSpa" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Loisirs
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="#">Excursions</a></li>
                        <li><a class="dropdown-item" href="#">Activités</a></li>
                        <li><a class="dropdown-item" href="#">Bien-être</a></li>
                        <li><a class="dropdown-item" href="#">Pour les enfants</a></li>


                    </ul>
                </li>
            </ul>
        </div>

        <!-- Bouton Réserver -->
        <div class="d-none d-lg-block">
            <a href="#" class="btn btn-luxury">RÉSERVEZ MAINTENANT</a>
        </div>
    </div>
</nav> #}

{# .................    NAVBAR 4  ........................... #}

<nav class="navbar navbar-expand-lg navbar-dark fixed-top custom-navbar">
<!-- Logo -->
                      <a class="navbar-brand" href="{{ path('app_home_page') }}">
                          <img src="{{ asset('uploads/images/LOGO BEIGE XXL.png') }}" alt="Logo" class="logo-navbar">
                      </a>
    <div class="container">
       

        <!-- Burger menu -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarLuxury"
                aria-controls="navbarLuxury" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Menu -->
        <div class="collapse navbar-collapse justify-content-center" id="navbarLuxury">
            <ul class="navbar-nav">

                {# Menus fixes #}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navHebergement" role="button" data-bs-toggle="dropdown">
                        La Villa
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="#">Le domaine</a></li>
                        <li><a class="dropdown-item" href="#">Les chambres</a></li>
                        <li><a class="dropdown-item" href="#">Les options</a></li>
                    </ul>
                </li>

                {# <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navRestaurants" role="button" data-bs-toggle="dropdown">
                        Activités & excursions
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="#">Restaurant 1</a></li>
                        <li><a class="dropdown-item" href="#">Bar Lounge</a></li>
                    </ul>
                </li> #}
                {# <li class="nav-item">
                    <a class="nav-link" href="#">Shopping</a>
                </li> #}
                

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navSpa" role="button" data-bs-toggle="dropdown">
                        Activités & excursions
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="#">Activités</a></li>
                        <li><a class="dropdown-item" href="#">Excursions</a></li>
                        <li><a class="dropdown-item" href="#">Bien-être</a></li>
                        <li><a class="dropdown-item" href="#">Pour les enfants</a></li>
                    </ul>
                </li>
                {# Catégories dynamiques avec sous-catégories #}
                {% if categories is defined %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="categoriesDropdown" role="button"
                           data-bs-toggle="dropdown" aria-expanded="false">
                            Shopping
                        </a>
                        <ul class="dropdown-menu">
                            {% for category in categories %}
                                <li class="dropdown-submenu position-relative">
                                    <a class="dropdown-item dropdown-toggle" href="#">
                                        {{ category.name }}
                                    </a>
                                    {% if category.subCategories|length > 0 %}
                                        <ul class="dropdown-menu">
                                            {% for subCategory in category.subCategories %}
                                                <li>
                                                    <a class="dropdown-item"
                                                       href="{{ path('app_home_product_filter', {'id': subCategory.id}) }}">
                                                        {{ subCategory.name }}
                                                    </a>
                                                </li>
                                            {% endfor %}
                                        </ul>
                                    {% endif %}
                                </li>
                            {% endfor %}
                        </ul>
                    </li>
                {% endif %}

                {# Menu Admin #}
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-warning" href="#" id="adminDropdown" role="button"
                           data-bs-toggle="dropdown">
                            Admin
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ path('app_category') }}">Categories</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_category_new') }}">Add a Category</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('app_sub_category_index') }}">Subcategories</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_sub_category_new') }}">Add a subcategory</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('app_product_index') }}">Products</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_product_new') }}">Add a product</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('app_user') }}">User</a></li>
                        </ul>
                    </li>
                {% endif %}
            </ul>
        </div>

        <!-- Connexion / Déconnexion + Bouton Réserver -->
        <div class="d-flex align-items-center gap-2">
            {% if app.user %}
                <span class="text-white small">Bonjour, {{ app.user.firstname ?? app.user.lastname }}</span>
                <a href="{{ path('app_logout') }}" class="btn btn-outline-light btn-sm">Déconnexion</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-outline-light btn-sm">Connexion</a>
            {% endif %}
            {# <a href="#" class="btn btn-luxury">RÉSERVEZ MAINTENANT</a> #}
            {# ...........  BARRE DE RECHERCHE ................. #}

{# <form class="d-flex me-3" role="search" action="{{ path('app_search') }}" method="get">
    {# <input class="form-control me-2" type="search" name="q" placeholder="Rechercher..." aria-label="Search">
    <button class="btn btn-outline-light" type="submit">Rechercher</button> #}
<div class="ms-auto">
    <form class="search-form d-flex" role="search">
        <input class="form-control me-2 search-input" type="search" name="q" placeholder="Rechercher..." aria-label="Search">
        <button class="btn search-btn" type="submit">Rechercher</button>
    </form>
</div>
        </div>
    </div>
</nav>
